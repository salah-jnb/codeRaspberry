# HTTP client for the FastAPI backend
httpx>=0.27,<1.0

# Serial I/O for Nextion and Arduino
pyserial>=3.5

# GPIO touch sensor (Pi runtime)
gpiozero>=1.6
# Pin-factory backend: gpiozero needs lgpio on recent Pi kernels (6.x) —
# RPi.GPIO no longer works there. lgpio does NOT pip-build on the Pi (needs
# swig + liblgpio-dev), so use the system package and link it into the venv:
#   sudo apt install -y python3-lgpio
#   SP=.venv/lib/python3.13/site-packages
#   ln -sf /usr/lib/python3/dist-packages/lgpio.py "$SP/"
#   ln -sf /usr/lib/python3/dist-packages/_lgpio.cpython-313-aarch64-linux-gnu.so "$SP/"

# Config loading
python-dotenv>=1.0

# Wake-word detection (offline streaming STT)
vosk>=0.3.45

# Hybrid wake-word WebSocket client (Pi → backend → Azure Speech streaming).
# Only used when HYBRID_WAKE_WORD_ENABLED=1; otherwise pure Vosk path runs.
websockets>=12.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
