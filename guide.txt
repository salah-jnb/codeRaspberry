// initialiser environnement
source .venv/bin/activate
python -m app.main
// Micro (OBLIGATOIRE si Vosk heartbeat RMS peak=0)
// Test manuel : arecord -D plughw:3,0 -f S16_LE -r 16000 -c 1 -d 5 /tmp/t.wav && sox /tmp/t.wav -n stat | grep Maximum
// Si Maximum > 0.15 en parlant, dans .env :
//   RESPEAKER_DEVICE=plughw:3,0
// (pipewire seulement si wpctl set-default pointe vers ReSpeaker / source_noise_suppressed)
export RESPEAKER_DEVICE=plughw:3,0

// --- Backend PC (hostname mDNS, meme Wi-Fi) ---
// Sur Windows : hostname  →  ex. SALAH_DESKTOP  →  URL Pi : http://SALAH_DESKTOP.local:8000
export BACKEND_URL=http://SALAH_DESKTOP.local:8000
export BACKEND_TIMEOUT=120
// Test depuis le Pi AVANT app.main :
//   ping -c 2 SALAH_DESKTOP.local
//   curl -s --max-time 5 http://SALAH_DESKTOP.local:8000/
// Si ping/curl echouent : sudo apt install avahi-utils && fallback IP :
//   export BACKEND_URL=http://192.168.69.185:8000

python -m app.main

//cablage ecran
RX (Jaune/) -> Pin 8 (GPIO 14 - TX) du Raspberry Pi.

TX (Bleu) -> Pin 10 (GPIO 15 - RX) du Raspberry Pi.

// checks hardware fiables (evite faux positifs)
// adapte ces valeurs a ton cablage reel
// Nextion via pins GPIO RX/TX 8/10
export NEXTION_PORT=/dev/serial0
export NEXTION_REQUIRE_GPIO_UART=1


// Audio sortie (carte ALSA exacte)
// verifie avec:                 
export AUDIO_CARD_INDEX=1
// optionnel: label texte si besoin
// export AUDIO_CARD_LABEL=USB Audio Device




sudo nmcli connection up aa

$conns = Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique
foreach ($p in $conns) { if ($p -gt 0) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue } }
Start-Sleep -Seconds 2
Set-Location "c:\pfe\Distributeur-Des-Service-Python"
// --- PC Windows : relancer le backend (port 8000) ---
// $conns = Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique
// foreach ($p in $conns) { if ($p -gt 0) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue } }
// Start-Sleep -Seconds 2
// Set-Location "c:\pfe\Distributeur-Des-Service-Python"
// python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload



# 1. Sauvegarder ton .env local
cp .env .env.local.bak

# 2. Mettre tes modifs locales de côté (git stash)
git stash push -m "local Pi .env" -- .env

# 3. Pull tranquille
git pull

# 4. Récupérer tes modifs (peut générer un conflit si la version distante a aussi changé)
git stash pop